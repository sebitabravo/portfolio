# Portfolio audit cleanup

## Goal
Address the user's independent portfolio audit in evidence-backed stages: fix the production dependency-audit failure first, then correct confirmed localization/accessibility defects, remove only demonstrably dead weight, and make CI/docs reflect actual behavior. Preserve the current design and all existing uncommitted branch work. Do not commit or push unless the user separately authorizes it.

## Baseline and reconciliation
- Continue on `odd/portfolio-design-architecture`, HEAD `a6f27dad900c0e6925e74c6c551927322f63a339`. The worktree is already dirty with the previous portfolio refactor; preserve all current changes. No reset/clean.
- The initial audit reconciliation incorrectly recorded `components.json` as absent: it is present in the current worktree. Correction (2026-09-24): `lighthouse-scores.png` was never absent — it stayed tracked in git while `.gitignore` ignored it. Both files were resolved in `portfolio-final-polish.md` (T3): `components.json` deleted, the PNG untracked. The reported `LanguageToggle` slug bug is fixed and tested. The scroll-reveal-without-JS concern was refuted; keep its visible-by-default behavior.
- Initial high findings were the vulnerable `devalue` 5.8.1 override and hardcoded Spanish blog copy; both were resolved in the completed tasks below. These are historical baseline findings, not current defects.
- Evidence collected read-only: `pnpm audit --prod` exits 1 with GHSA-9rgm-9g3h-6x36 (`devalue` via Astro, patched `>=5.9.2`); `pnpm why devalue` resolves `5.8.1`; official npm registry metadata reports `devalue@5.9.2` published `2026-08-27T14:16:15.893Z`, more than 72 hours before the observed system date `2026-09-23T14:53:35Z`.
- Other candidates require deliberate handling: design-token CSS values are asserted by tests and described as semantic palette values in `DESIGN.md`; the CI build-script allowlist has inconsistent entries and precedence must be verified against pnpm 10.30.1 docs before consolidation; `openspec/changes/security-headers` contains historical verification artifacts and must not be deleted or manually moved without an explicit SDD/archive decision; public certification PDFs require checking tracked status and direct URL references before deletion.

## Staged tasks
- [x] Remediate the vulnerable `devalue` override with a registry-age-verified patched release; regenerate `pnpm-lock.yaml`, confirm actual resolution, and get `pnpm audit --prod` to exit 0. `package.json` now overrides exact `5.9.2`; only corresponding override/resolution/Astro/snapshot lock entries changed. pnpm 10.30.1 frozen install passed with scripts ignored, `pnpm why devalue` shows only 5.9.2, `pnpm audit --prod` exits 0/no known vulnerabilities, lockfile-only frozen check and diff check passed. Release timestamp `2026-08-27T14:16:15.893Z` is >72 hours old at observed system date. No commit.
- [x] Make blog copy and breadcrumb labels use the i18n source of truth in both locales (`LocalizedBlogIndex` and `LocalizedBlogPost`); add rendered ES/EN dictionary-copy tests and source coverage for the empty state. Added `nav.breadcrumb` to both dictionaries; index title/description/intro/empty, breadcrumb names/links, and article back/home labels use dictionary values. RED: Vitest 1 contract test failed; Playwright 3 ES breadcrumb assertions failed. GREEN: clean build (149 Astro files/0 diagnostics, 90 pages), full Vitest (18 files/103 tests), blog Playwright 48/48 across Chromium/Firefox/WebKit, and `pnpm audit --prod` all passed. Independent review found no defect; empty state is protected by a source-contract assertion because content is nonempty, not by rendering an empty collection.
- [x] Fix WCAG 2.2.2 behavior: pause the certification carousel on keyboard focus and resume safely on focus leaving; add browser coverage. Also stop/resume when the reduced-motion preference changes at runtime, abort pending `scrollend` listeners during Astro teardown, and update the stale performance source-contract assertion. Recomputed the inline-script SHA-256 and replaced only the carousel token in `vercel.json`, preserving prior policy edits. RED: 3 focus E2E failures/3 reduced-motion passes; later 2 Vitest source contracts failed and 3 runtime reduced-motion cases failed, as expected. GREEN: build passed with 90 pages and CSP parity; full Vitest 18 files/104 tests passed; carousel Playwright 9/9 across Chromium, Firefox, WebKit; production audit remains clean from prior stage. Independent post-change review found no defects at that stage. Final full Playwright later exposed a WebKit-only carousel failure: in the diagnostic, motion preference was not reduced, IntersectionObserver reported intersecting entries, rAF callbacks ran, and there was no hover/focus/page error, yet `scrollLeft` remained 0 after 1.2s. Resolved with a fractional scroll accumulator synchronized to actual `scrollLeft` on auto-scroll start/stop; focus/hover/reduced-motion guards and cleanup remain. Focused WebKit carousel 3/3 and final full Playwright 273/273 (Chromium/Firefox/WebKit) pass.
- [x] Reconcile package/CI configuration: replaced unsupported duplicated build-script allowlists with pnpm 10.30.1's canonical workspace `allowBuilds` map (`@tailwindcss/oxide`, `esbuild`, `sharp`); removed only the undocumented `.npmrc`/workspace/package.json duplicates; retained all security overrides (the diff confirms no removals/changes besides the earlier devalue fix). CI now runs `pnpm build:ci` after its sole `pnpm astro check`. RED/GREEN config tests: 3 failed before config change, 3 passed after. Frozen install with scripts ignored passed; `pnpm config list` no longer shows the removed local custom key (global `ignore-scripts=true` remains). `pnpm astro check && pnpm build:ci` passed; 90 pages, bundle and CSP parity verified. `git diff --check` passed. Lockfile diff remains only the earlier devalue 5.8.1→5.9.2 resolution. Independent reviewer found no functional issue; noted tests do not snapshot every override, while current diff inspection confirms all remain unchanged.
- [x] Make LHCI audit the current checked-out build on PR, main push, and manual runs; retain three-run error-level thresholds and blocking `continue-on-error: false`, update README to match. Uses the LHCI built-in server via `collect.staticDistDir: "./dist"` and explicit localhost ES/EN URLs. RED: 3 config/workflow/doc tests failed against old production URLs/no build/informational README; GREEN: 3/3. `pnpm build` passed (90 pages, 0 Astro diagnostics, bundle/CSP parity); both `dist/index.html` and `dist/en/index.html` exist; diff-check clean. Independent review found no defects. LHCI action itself was not executed locally; workflow runtime remains the final live audit. Branch-protection required status remains externally managed.
- [x] Add immutable SHA pins for GitHub Actions with verified upstream commit references and a maintainable update mechanism. All 8 `uses:` occurrences across both workflows now use full 40-character SHAs with same-line release comments: checkout v4.2.2, pnpm/action-setup v4.0.0, setup-node v4.4.0, upload-artifact v4.6.2, treosh LHCI 12.6.2. Added `.github/dependabot.yml` for weekly GitHub Actions updates; tests enforce all pins and update schedule. RED 3/3, GREEN focused 6/6; diff-check passed; independent review found no concrete defect. Active LSP rerun found no line-length diagnostics; only two non-blocking `zizmor artipacked` warnings at checkout steps, not changed in this task. GitHub repository/organization action-SHA enforcement and Dependabot activation are external settings and remain to be confirmed.
- [x] Complete CSP inline-script classification: parse actual `src`/`type` attributes across quoted values and `>` characters; count only classic/module/recognized JavaScript MIME types, not non-executable data blocks such as JSON-LD, JSON, import maps, or speculation rules. Add regressions for `data-*` false positives and script types. Three test-first rounds recorded RED/GREEN; final full Vitest 21 files/121 tests passed, `pnpm verify:csp` passed on 90 HTML files/636 executable inline scripts/10 hashes, and diff-check passed. Independent review found no concrete defect in scoped parsing. Regex discovery could theoretically match `<script>` inside HTML comments; reviewer found no such case in current source but did not inspect generated output. `dist` parity check passed; retain this as a bounded parser limitation, not a claimed failure.
- [x] Localize site-shell navigation/footer accessible names in `Header.astro`, `Footer.astro`, and `LanguageToggle.astro`; test server-rendered ES/EN copy and client-updated language-toggle label. Added `nav.mainNavigation`, `nav.brandHome`, `language.toggleLabel`, `footer.professionalProfiles`, and `footer.links` in both dictionaries; components consume these values, including localized client-side toggle templates. RED 4 failed, GREEN 4; build initially exposed exactly one missing/one obsolete inline-script CSP hash. Replaced only the observed hash, then full build passed (90 pages, 0 Astro diagnostics), focused Playwright passed 6/6 across Chromium/Firefox/WebKit, Vitest 4/4, CSP parity 90 HTML/636 scripts/10 hashes, diff-check clean. Independent review found no remaining defect after test correction to switch locales.
- [x] Localize project/work/contact UI labels in `Projects.astro`, `ExperienceItem.astro`, and `Contact.astro`; preserve user-authored/data descriptions. Added dictionary keys for project kicker/evidence/technologies, work evidence, email subject, three contact-card descriptions, and CV helper in both locales; contact subject remains encoded. RED 4 failed/4 passed, GREEN 8/8. Build passed (90 pages, 0 Astro diagnostics), focused Playwright passed 6/6 across Chromium/Firefox/WebKit, CSP parity passed (90 HTML/636 scripts/10 hashes), diff-check clean. Independent reviewer found no concrete defect and confirmed existing data bindings remain.
- [x] Localize 404 copy and blog-post navigation landmark in `LocalizedNotFoundPage.astro` and `LocalizedBlogPost.astro`; test visible/rendered ES/EN behavior. Added `error404.description/heading/message/backHome` and `blog.postNavigation` in both dictionaries; kept exact 404 wording, title/canonical/noindex/home URLs, and article content. RED 4 failures, GREEN 12/12 focused Vitest; build 90 pages/0 diagnostics; browser checks 18/18 across three engines; CSP 90 HTML/636 scripts/10 hashes; diff-check clean. Updated existing blog accessibility expectation; independent review found no defect.
- [x] Fix the mobile navigation disclosure's outside-click focus theft: closing after an outside pointer click must not steal focus from the clicked target; Escape must still restore focus. Preserve its normal link-tab behavior and do not alter nested navigation semantics. TDD RED focused Playwright failed on focus theft; source now passes `closeAllDropdowns(false)` only for outside click. Build first reported the exact missing/unused bundled-script hash; after one token replacement, build passed (90 pages, 0 Astro diagnostics, CSP parity 90 HTML/636 scripts/10 hashes). Regression passes 3/3 on Chromium/Firefox/WebKit after replacing a pointer-click `<button>` fixture with a text input because WebKit does not consistently focus buttons on click. Diff-check and independent review pass.
- [x] Fix theme/language radio-menu keyboard and focus behavior: Tab/Shift+Tab exit without focus traps; arrow/Home/End navigation maintains one roving tab stop and ARIA radio state; same-value selection restores trigger focus; language controller cleans up and reinitializes after Astro swaps. Extracted the client controller from the 270-line Astro component into `src/scripts/language-toggle.ts` to stay under the 250-line source cap; added five happy-dom tests and updated copy source contracts. Build-observed CSP delta was confirmed by `pnpm verify:csp`, and only the exact two stale hashes were replaced. GREEN: focused Vitest 25/25, `pnpm build` passed (157 Astro files/0 diagnostics, source limits, 90 pages, bundle limits, CSP parity 90 HTML/636 scripts/10 hashes), focused Playwright 9/9 across Chromium/Firefox/WebKit. Independent review found no concrete defects. No commit/push.
- [x] Remove only verified dead code/assets. Current-tree audit confirmed `translations` and `TranslationKeys` had no consumers outside `src/lib/i18n/index.ts`; `language-preference` was written but never read. Added test-first coverage and removed the dead localStorage write/helper; made the two i18n implementation details module-private while retaining `Locale`, `locales`, `defaultLocale`, and `getTranslations`. RED: one test failed because init wrote `"es"`; GREEN: focused Vitest 11/11, build 157 Astro files/0 diagnostics, source limits, 90 pages, bundle and CSP parity pass (90 HTML/636 scripts/10 hashes). Independent review found no defect. Preserved ambiguous locale keys, historical PDFs, and CSS token scaffolding; no user assets deleted. No commit/push.
- [x] Refresh README and architecture docs against the current tree; correct design-system inaccuracies; decide style-tooling scope. README now documents the actual `src/content.config.ts`, blog-only MDX collection, split typed `src/lib/data/*` modules and facade, routes, current CI/LHCI and verification commands. `DESIGN.md` now matches light-first/dark hero palettes, Manrope nav, actual responsive clamps and typography line-heights, progressive motion, and 1200px container. `docs/architecture/content-strategy.md` records current blog/domain-data split and defers wholesale migration/CMS pending an editorial need. Decision: add no formatter/linter dependency now; none is configured and no concrete recurring format/lint gap was found; revisit with evidence. Baseline docs contracts 11/11; after changes 11/11, plus final focused page-smoke 8/8; diff-check passes. Independent reviewer found no inaccuracies after correcting the typography table and responsive claims. No dependency added.
- [x] Run final local quality gates and report the current worktree. `pnpm build` passed (157 Astro files/0 diagnostics, source limits, 90 pages, bundle and CSP parity: 90 HTML/636 executable scripts/10 hashes); `pnpm audit --prod` exited 0/no known vulnerabilities; `pnpm test` passed 23 files/143 tests; coverage passed at 91.38% statements/lines, 82.58% branches, 95.78% functions; full Playwright passed 273/273 across Chromium/Firefox/WebKit; link validation observed 6/6 HTTP 200; docs/page-smoke checks passed (11/11 and 8/8); `git diff --check` clean. Final independent reviews found no concrete code defects or remaining doc inaccuracies. Branch `odd/portfolio-design-architecture`; 54 tracked modifications and 27 untracked files remain intentionally unstaged; no commit/push.
- [ ] Observe hosted LHCI against a PR build after a user-authorized PR/CI trigger; the hosted action was not run locally. Branch-protection merge requirements remain external and are not claimed. Two non-blocking `zizmor artipacked` warnings remain at workflow checkout steps.

## Working rules
- Use strict test-first for behavior and regression fixes. One writer at a time. Delegate multi-file writers and command-running verification agents with narrow edit surfaces.
- Update this feature record, Engram mirror, and visible `todo` after each task transition/material plan change.
- `pnpm build` is explicitly authorized despite its `rm -rf dist`; do not use that as permission for any other cleanup/destructive git operation.
- The user explicitly requested archiving `openspec/changes/security-headers` after this cleanup. Preserve its artifacts until the native SDD archive phase runs; do not move or delete them manually.
- No commit/push: the user previously explicitly prohibited both absent further authorization.

## Progress
- 2026-09-23: Read all three user-provided `/tmp/portfolio-audit/*.md` reports and commissioned separate read-only config and dead-code scouts plus a current-tree reconciliation scout. Confirmed devalue audit failure, patched release age, live blog copy drift, and focus-pause gap. Discarded stale findings already fixed or absent in this branch. First stage completed: exact devalue 5.9.2 override and lock update; frozen install with scripts ignored, `pnpm why devalue`=5.9.2, `pnpm audit --prod` clean, no unrelated lock churn. Stage 2 completed test-first: blog copy and navigation labels now read both dictionaries; `nav.breadcrumb` added to both locales. Initial test import failure (JSON import attributes) was fixed by parsing dictionary JSON from `readFile`; RED was 3 Spanish breadcrumb failures and 1 source-contract failure. Focused GREEN was 8 Vitest + 6 dictionary Playwright cases; delegated build+full Vitest+blog Playwright+audit then passed: Astro 149 files/zero diagnostics, 90 pages, 103 Vitest, Playwright 48/48 across three engines, audit clean. Independent review found no code defect; the empty-state case is source-contract tested only because no empty content collection exists.
Stage 3 completed. Added focus pause/resume and safe descendant focus transitions; runtime preference changes now stop/restart only when all viewport/hover/focus/visibility guards allow it; pending `scrollend` listeners share the AbortSignal. The stale performance contract now checks semantic conditions. CSP token swap preserves all existing edits and `pnpm build` passes its 90-page/90-HTML parity check. TDD RED/GREEN as above; independent full Vitest passed 18 files/104 tests, carousel E2E passed 9/9 across Chromium/Firefox/WebKit, independent reviewer found no defect. Stage 4 completed: config follows documented pnpm 10.30.1 settings and CI avoids duplicate `astro check`; final validation and reviewer notes are recorded in the checklist above. Stage 5 completed per the user's decisions: Lighthouse now builds and audits the checked-out tree on PR/main/manual triggers, with `staticDistDir: ./dist`, local ES/EN URLs, and unchanged error-level thresholds/`continue-on-error: false`; README now accurately distinguishes workflow-job failure from external branch protection. RED/GREEN config/workflow/doc tests 3/3; build passed with 90 pages, no Astro diagnostics, bundle and CSP parity; local route files exist; diff-check clean. Independent review found no defect. LHCI action was not run locally, so hosted action runtime remains to be observed in CI. Stage 6 completed: the five action releases listed in the checklist above are SHA-pinned with same-line Dependabot-readable release comments; the existing workflow action inputs and behavior remain unchanged by the pin-only hunks, and `.github/dependabot.yml` requests weekly GitHub Actions updates. RED/GREEN tests: 3 failed before, focused pin/Lighthouse tests 6 passed after; diff-check clean; independent review found no defect. Re-ran active LSP on both workflow files as requested: previous line-length findings do not recur; two current non-blocking `zizmor artipacked` warnings remain at the checkout steps, with no modifications made. Stage 7 completed test-first. First attribute scanner RED 1/10, GREEN 11 then 12; quote-aware/real-attribute parser RED 3/13, GREEN 17; executable-type classification RED 1/17, GREEN 18. Final full Vitest passed 21 files/121 tests; `pnpm verify:csp` passed against 90 HTML files with 636 executable inline scripts and 10 unique hashes; diff-check clean. Independent review found no concrete defect in scoped behavior: actual first `src`/`type` attributes, quoted `>` handling, no/empty/module/recognized JavaScript MIME types, and exclusion of typed data blocks are covered. A regex scan could theoretically count `<script>` text inside comments; reviewer found no source evidence, and current built-dist parity passed, so this remains an explicitly bounded limitation. The `src/` suggestion was checked and is not a bug. Stage 8 exploration completed via two read-only scouts. Localization candidates and menu findings are recorded above. Batch 1 complete: Header/Footer/LanguageToggle accessible names are dictionary-backed in ES/EN, the client-updated language label uses locale templates, and e2e coverage switches to the other locale and checks the target label. RED 4, GREEN 4; build after the source change showed one exact missing/unused CSP hash pair; after replacing only that observed hash, build passed 90 pages/0 Astro diagnostics, Playwright 6/6 across three engines, CSP parity 90 HTML/636 inline scripts/10 hashes, focused diff-check clean; independent review found no defect. Batch 2 complete: Projects/ExperienceItem/Contact UI copy is dictionary-backed in both locales, including previously English-only `PROJECT` kicker and localized mailto subject. Unit tests 8/8; build 90 pages/0 diagnostics; focused Playwright 6/6 across three engines; exact CSP parity and diff-check pass; independent review found no defect. Batch 3 complete: the 404 component reads all visible/description copy from dictionaries and Spanish article navigation now has a localized landmark; existing blog accessibility assertions use `t.blog.postNavigation`. TDD RED 4, GREEN 12; build 90 pages/0 Astro diagnostics; focused browser checks 18/18 across Chromium/Firefox/WebKit; Vitest 12/12; CSP parity, diff-check, and independent review pass. All three copy batches are complete. Menu exploration confirms three distinct controllers: a plain navigation disclosure plus two role=menu radio controls. Split follow-up into the concrete mobile-disclosure outside-click focus bug, then specialized radio-menu Tab/focus/lifecycle behavior. Keep nav links in normal sequential tab order; do not merge controllers or alter nested landmark semantics without evidence. Mobile-disclosure outside-click focus batch completed: RED confirmed focus theft, and outside close now avoids restoring focus; Escape still restores trigger focus. First post-change build revealed one exact missing/unused CSP hash pair; after replacing only that build-observed token, build passed 90 pages/zero diagnostics and CSP parity 90 HTML/636 scripts/10 hashes. The cross-engine focus regression passes 3/3; initial WebKit-only failure was caused by pointer clicks not focusing the test button consistently, so the fixture now uses a text input. Independent review and diff-check pass. Theme/language radio-menu stage closed: focused Vitest 25/25; final `pnpm build` passed (157 Astro files/0 diagnostics, source limits, 90 pages, bundle and CSP parity 90 HTML/636 inline scripts/10 hashes); focused Playwright passed 9/9 across Chromium, Firefox, and WebKit; independent review found no concrete defects. Dead-code stage closed: focused Vitest 11/11, build 90 pages with source/bundle/CSP checks passing, and independent review clean. Preserved ambiguous locale keys, historical PDFs, and tested CSS tokens. Documentation stage closed: README/architecture reflect the current routes, content split, CI checks and style-tooling decision; DESIGN values now match CSS. Focused docs tests 11/11, page-smoke 8/8, diff-check and independent factual review pass. Final local gates passed: build 90 pages/157 Astro files/0 diagnostics with source/bundle/CSP parity; production audit clean; Vitest 23 files/143 tests and coverage thresholds pass; Playwright 273/273 on Chromium/Firefox/WebKit after fixing WebKit subpixel carousel scrolling; link validation 6/6 HTTP 200; diff-check clean. Final reviewer found no defects/doc inaccuracies. Hosted LHCI awaits a user-authorized PR/CI trigger; branch-protection status is external. Two non-blocking `zizmor artipacked` warnings remain. Branch `odd/portfolio-design-architecture` has 54 modified tracked files and 27 untracked, intentionally unstaged. No commit/push.
