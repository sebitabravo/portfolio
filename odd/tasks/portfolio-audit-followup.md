# Portfolio audit follow-up

## Goal
Close the remaining actionable findings from the architecture/quality, accessibility/performance, and best-practices reports, preserving the site's identity, bilingual behavior, accessibility, SEO, responsive behavior, and static-site deployment.

## Authorization and decisions
- The user explicitly authorized implementing all proposed follow-up groups and asked that no actionable item be left pending.
- Extend existing PR #37 on branch `refactor/portfolio-theme-tokens-01`. It is the user's approved single-PR `size:exception`; keep it open and do not merge.
- Asset decision: this is a static portfolio, so migrate project screenshots and the rendered ExperienceItem logos to `src/assets`, and use Astro `Picture` in Projects, ExperienceItem, and the optional CertificationCarousel logo branch. Allow Astro-generated image URLs and update every repository reference; old `/screenshots/...` and `/experience/...` paths are not treated as stable public APIs. Preserve source quality, accessibility text, responsive behavior, and visual crop as closely as Astro allows. CertificationCarousel currently receives no logo files; keep that optional path typed for Astro-managed local metadata. Use AVIF sources with WebP fallbacks for rendered images; retain WebP for Open Graph/social metadata compatibility.
- Security-headers OpenSpec archive is explicitly out of this implementation scope: native `sdd-archive` is unavailable, and no manual archive is authorized.
- The user's report calls the ExperienceItem 14-prop observation a non-urgent nice-to-have, not part of the ordered implementation groups; do not widen scope into a component API redesign without a concrete need.

## Workflow and verification
- Strict TDD is the previously selected project mode. Record observed RED/GREEN for each behavior change; do not infer test evidence.
- Focused runners: `pnpm exec vitest run <focused test paths>` and `pnpm exec playwright test <focused spec paths>`.
- Full verification: `git diff --check && pnpm build && pnpm test && pnpm exec playwright test`.
- Use one delegated writer per bounded multi-file implementation task and a separate `gentle-ai-verify` pass; parent owns integration and commits.
- After each task: update this file, its Engram mirror, and the visible todo projection; create a Conventional Commit on the existing feature branch. Push task commits to update PR #37. No merge.

## Tasks
- [x] Add a localized accessible region name/role to CertificationCarousel and regression assertions; keep existing keyboard/focus behavior. **Evidence:** RED 2/2 failed as expected; GREEN 2/2 passed after safe build; independent build/limits/bundle/CSP verification passed; full Chromium carousel spec 5/5 passed. **Commit:** `60faeba`.
- [x] Make hero motion/WebGL lifecycle respond to live `prefers-reduced-motion` changes in both directions, including initial reduced-motion and teardown; preserve animation/resource cleanup. **Evidence:** RED 2 live-transition tests failed before implementation; later coarse-pointer RED reproduced fallback; final Vitest 31/31, Chromium 5/5, Astro check, source limit, build, bundle and CSP all passed. In-flight loads are invalidated before GSAP/WebGL activation, active scenes are reverted, preference listeners are removed; `portfolio-motion.ts` is 294 lines. **Commit:** `01b57eb`.
- [x] Centralize static hreflang alternate construction and reuse the exported `Locale` type in BlogCard and other local duplicate unions; preserve alternate URL ordering, x-default, and current route behavior. **Evidence:** RED 4 helper tests failed before implementation; GREEN Vitest 15/15; independent route tests 34/34. Astro check, source limits, 90-page build, bundle, CSP, and diff checks passed. Preserved locale-first ordering and Spanish x-default. **Commit:** `c89f6a2`.
- [x] Move project screenshots and rendered ExperienceItem logos to Astro-managed assets; migrate all three named components (`Projects`, `ExperienceItem`, `CertificationCarousel`) to `astro:assets` Picture with AVIF/WebP output; update metadata helpers, mapping, Vercel cache rules, and tests after checking direct-reference hazards repository-wide. **Evidence:** Repository reference map covered all 12 screenshot variants, 3 experience logos, 6 MDX keys, Vercel, tests, and confirmed no OG-route URL. RED observed for prior-path unit/E2E contracts; final Vitest 19/19 and Chromium 47/47 passed. Astro check, 90-page build, limits, bundle, CSP, and `git diff --check` passed. Built sources report cards 1600×1000, blog social sources 1200×750 (Wenuke 1710×929), and experience logos 768×166/225×225/334×151. AVIF is offered with responsive WebP fallbacks; social metadata stays WebP; missing keys preserve the old no-image fallback. **Commit:** `099bf80`.
- [x] Replace brittle source-text/line-count assertions in JSON-LD and CSS contract tests with rendered output, compiled CSS, and observable behavior contracts. Extract the `noindex` WebPage-schema branch into a pure helper so it can be behavior-tested; retain only the import-facade structural assertion because it describes source ownership rather than runtime output. **Evidence:** Replacement ES/EN JSON-LD and compiled-CSS characterization specs passed before narrowing old assertions; the pure schema helper had RED 2/GREEN 3. Independent full Vitest passed 162 tests; Astro check, limits, 90-page build, bundle, CSP, and diff checks passed; rendered JSON-LD/CSS specs 9/9 passed. Only the documented import-facade structural test remains. **Commit:** `584544f`.

- [x] Change only `tests/project-blog-destinations.spec.ts` to import `getProjects` directly from `src/lib/data/projects.ts`; preserve all end-to-end assertions. **Evidence:** The previous full run failed before browser totals on the `.webp` parse path through the barrel; read-only import tracing confirmed it. After the one-line import change, focused Chromium spec passed 2/2 with no parse error. **Commit:** `3c73b34`.
- [x] Give the certification-carousel region a unique bilingual accessible name so it does not duplicate the surrounding `Certificaciones` / `Certifications` section landmark. Update the accessible-name regression; preserve the named-region behavior. **Evidence:** RED 2 locale tests failed before the label change; GREEN focused carousel and home accessibility checks passed 7/7. Independent Astro/build/limits/bundle/CSP check passed; Axe reported zero violations for both locales. **Commit:** `1537205`.
- [x] Update `tests/judgment-day.spec.ts` responsive image assertion from the retired `-800.webp` path to Astro-generated `/_astro/` AVIF/WebP responsive candidates; do not change image implementation. **Evidence:** The previous complete run failed this assertion on all three engines; independent focused run passed 9/9 across Chromium, Firefox, and WebKit, requiring both formats at 800w and 1600w. **Commit:** `7818e37`.
- [x] Fix `scripts/validate-project-links.ts` so running it under plain Node ESM does not traverse asset-bearing `.webp` modules. The hosted `validate:links` step on `96d2155` failed with `ERR_UNKNOWN_FILE_EXTENSION` for `src/assets/experience/mimasoft.webp`; switched to the direct `projects` module without changing validation semantics. **Evidence:** Writer and independent validator both ran `pnpm validate:links`; all six unique URLs returned HTTP 200, and `git diff --check` passed. **Commit:** `2392415`.
- [x] Stabilize the hosted WebKit responsive-image smoke test without changing production behavior. **Evidence:** Hosted CI `35966330005` showed `scrollIntoViewIfNeeded` timing out in this image-only case while global smooth scrolling was enabled, then passing on retry. The test now emulates reduced motion locally, preserves lazy-loading/format/size/alt/naturalWidth assertions, and passes with retries disabled: WebKit 1/1 and all browsers 3/3. **Commit:** `94eacc0`.
- [ ] **In progress — final integration:** Full independent verification after Task 11 passed: Astro 162 files/0 diagnostics; source limits; build 90 pages; bundle 8 chunks (largest 511.4 KiB raw/126.9 KiB gzip); CSP 90/636/10; Vitest 23 files/162 tests; coverage 92.33% statements/lines, 84.93% branches, 96.07% functions; Playwright 339 with zero retries; link validator 6/6 HTTP 200; `git diff --check`. Local diff to `main` is 6,168 lines/116 files (4,752 additions + 1,416 deletions). GitHub's previous head `b19d453` reports 6,175 lines/116 files (4,754 + 1,421); the PR body remains stale at 5,158/84. Record Task 11, refresh exact PR size/body, push, and confirm hosted checks on the final head. Keep PR open; no merge.

## Asset migration map
- Read-only repository audit found 12 screenshot WebPs (four project slugs with plain/800/1600 variants), three rendered ExperienceItem logos under `public/experience`, and no currently supplied CertificationCarousel logo files (`logo` is undefined). The only runtime screenshot consumers are project-card markup and blog metadata; six MDX image keys include four resolvable project keys and two missing images that must continue returning no image. Existing tests and `vercel.json` also mention public paths. `src/pages/og/[locale].svg.ts` has no screenshot reference.
- Use explicit static asset imports; do not construct import paths dynamically from runtime slugs. Verify source dimensions/aspect ratios with image metadata before choosing the asset source and responsive widths; preserve the current project-card crop/layout, metadata fallbacks, alt text, and cache intent where appropriate. The installed Astro `Image` component emits one format; `Picture` is required for AVIF plus a WebP fallback. Assert the generated AVIF source and WebP `<img>` fallback. `getImage` social metadata intentionally remains WebP.

## Baseline
- Existing PR #37 targets `main`, carries all prior portfolio work under explicit `size:exception`, and latest checks passed at `25999a2`.
- Existing local checks: build 90 pages/CSP parity 90 HTML, 636 executable inline scripts, 10 hashes; Vitest 23 files/151 tests; Playwright 276/276.
- Read-only mapping confirmed existing screenshot URLs in project/blog code and tests; no `/screenshots` reference was found in `src/pages/og/[locale].svg.ts`. External consumers cannot be proven by repository search; the user accepted changing URLs for this static portfolio.
