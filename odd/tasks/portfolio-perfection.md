# Portfolio perfection — limpieza, buenas prácticas y mantenibilidad

## Objective
Dejar el portfolio limpio, bien programado, funcional y fácil de mantener a mano, respetando buenas prácticas, sin cambiar identidad visual ni comportamiento ES/EN, accesibilidad, SEO, responsive y reduced-motion.

## Problem
Diagnóstico multi-agente 2026-09-25 (3 explorers read-only):
- Base sana (pipeline Astro static coherente, facade `data.ts` + `Localized*`), pero desorden acumulado + 4 críticos reales.
- Duplicado muerto `src/lib/tech-colors.ts` 311 líneas vs sistema vivo `tech-badges.ts`.
- Deriva es/en: `privacy.astro` duplicado ~300 líneas, `work.ts` duplica registros.
- Higiene: `odd/` infla PR #37 sin gitignore/vercelignore, basura local ignorada presente (`dist/`, `test-results/`, `playwright-report/`, `.astro/` logs, `lighthouse-scores.png`).
- Fragilidades funcionales: Analytics nunca renderiza pero paga deps+CSP, CSP 10 hashes exactos, WebGL al límite del budget, flake WebKit `animation-behavior.spec.ts:31`, OG SVG débil, coverage solo `src/lib`+`src/scripts`.

## Why
Usuario 2026-09-25: "dale nomas... realiza todo para que quede bien programado y que respete buenas practicas... para que este perfecto". Autoriza implementación completa local. Push/PR/merge siguen siendo decisión suya.

## Scope
- In: `src/`, `tests/`, `scripts/`, configs raíz, `.github/`, `.gitignore`, `.vercelignore`, `public/`, docs `README`/`DESIGN.md`/`docs/` mínimos para mantener.
- Out: `openspec/` (change `security-headers` completo, no tocar), historial git, `node_modules/`, publicar o mergear.

## Constraints
- No cambiar identidad visual ni copy sin autorización. Pixel-gate donde aplique.
- Preservar ES default sin prefijo + `/en/`, alternates, sitemap, JSON-LD, CSP verifier, budgets (limits/bundle/CSP).
- Un writer a la vez. Nada de 100 agentes en paralelo: un solo hilo de escritura para evitar conflictos. Exploración ya hecha, no repetir.
- Rama actual `refactor/portfolio-theme-tokens-01` con 6 archivos sucios (Header/toggles/header.css/base/hero). Primer paso: estabilizar eso antes de nueva deuda. Se continúa en esta rama extendiendo PR #37 que ya tiene `size:exception`.
- RDD off (default 2026-09-25: `gentle-ai review mode status` = off, assess medium/under_budget). No native review. Verificación ordinaria + assess por tier.
- 400 líneas es heurística de planificación, no cap de aceptación. No borrar espacios/comentarios útiles para ahorrar líneas. No omitir tests.

## TDD
- Resolved mode: STRICT ON. Source: `sdd-init/portfolio` Engram #1668 + `portfolio-audit-followup.md` + `portfolio-final-polish.md` (strict). Runner: `pnpm test` (`vitest run`), focused `pnpm exec vitest run <paths>`, E2E `pnpm exec playwright test <paths>`.
- Forward en cada delegación: modo + source + runner. Exigir RED observado antes de implementar, GREEN, luego REFACTOR. Nunca inventar evidencia.
- Full gate: `git diff --check && pnpm build && pnpm test && pnpm exec playwright test`. Gates rápidos por tarea según toque.

## Delivery
- Strategy: `ask-on-risk` (default). Forecast inicial >400 líneas acumuladas (solo P1+P4+P5 ya lo superan), pero se trabaja por slices en la misma rama/PR #37 que ya tiene `size:exception` autorizada. No se crea PR nuevo sin decisión explícita.
- Running count desde work-unit commits en esta rama. Registrar slice boundaries aquí.
- Skills: `work-unit-commits` + `chained-pr` resueltos por nombre de registry antes de planear/crear cualquier PR. Paths en registry Engram #1670.

## Authorized scope
- Cambio autorizado 2026-09-25 "dale nomas... realiza todo... para que este perfecto". Incluye editar tests protegidos (`lib-contracts`, `css-contracts`, `projects-script`) solo para mantener contratos tras mover código muerto a sistema canónico, con evidencia RED/GREEN.
- Commits work-unit en rama feature autorizados. Push, PR nuevo, merge: NO autorizados, decisión del usuario.

## Acceptance criteria
- `git status` limpio de basura trackeada, `git check-ignore` confirma ignorados.
- `pnpm build` verde (astro check 0, limits, bundle, CSP), `pnpm test` verde, Playwright relevante verde.
- Sin `tech-colors.ts`, sin duplicación privacy/work, Layout/Carousel dentro de presupuesto o con excepción documentada.
- Docs actualizados donde cambió convención (README lib/, translationKey, tokens).

## Applicable checks
- Per-task: `git diff --check` + runner enfocado (vitest o playwright según toque) + `pnpm build` cuando toque estilos/config/CSP.
- Full al cierre: `git diff --check && pnpm build && pnpm test && pnpm exec playwright test` + `gentle-ai review assess --cwd <repo> --json` informativo (RDD off).

## Tasks
- [x] P0 — Estabilizar sucio actual (Header/toggles/header.css/base/hero, 54+/179-). Verificar qué es, correr checks enfocados, un work-unit commit. Route: delegated writer (6 files → writer trigger). Checks: diff-check + vitest relevante + build.
- [x] P1 — Eliminar `src/lib/tech-colors.ts` muerto + podar bloque legacy en `tests/lib-contracts.test.ts`, canónico `tech-badges.ts`. TDD RED/GREEN. Route: delegated writer. Checks: `pnpm exec vitest run tests/tech-badges.test.ts tests/lib-contracts.test.ts` + build.
- [x] P2 — Higiene trackeada: `odd/` a `.gitignore`, `odd/`+`openspec/` a `.vercelignore`, mover `Sebastian_Bravo_CV.pdf` a `public/`, borrar dirs vacíos `public/experience/`+`public/screenshots/.gitkeep`, cuarentena `tests/capture-screenshots.spec.ts` fuera de testMatch, rename `judgment-day.spec.ts` → `regression-contracts.spec.ts`. Limpieza local ignorada (`dist/`, `test-results/`, `playwright-report/`, `.astro/*.log`, `lighthouse-scores.png`) sin commit. Route: delegated writer (2+ files). Checks: `git check-ignore`, vitest list, build.
- [x] P3 — Analytics muerto: confirmado (ver evidencia), quitados `@vercel/analytics`+`speed-insights` + entradas CSP/connect-src + re-hash 10→11 en mismo commit. Route: delegated writer. Checks: build + verify:csp + grep sin refs.
- [x] P4 — Privacy deduplicado: crear `LocalizedPrivacy.astro`, migrar `privacy.astro` + `en/privacy.astro` a wrapper delgado. Route: delegated writer. Checks: vitest + playwright smoke/lang + build. (`7c6070b`, ver evidencia abajo)
- [x] P5 — `work.ts` a patrón `Shared + Record<Locale>` como `projects.ts`, tipar retornos. Route: delegated writer. Checks: vitest data/contracts + build. (`5fb3661`, ver evidencia abajo)
- [x] P6 — Layout slim: extraer JSON-LD y scripts inline estables, re-hash `vercel.json` en mismo commit, mantener hashes 10/10. Route: delegated writer. Checks: build + verify:csp + playwright relevante. (`3ac099d`, ver evidencia abajo)
- [x] P7 — Carousel slim + tokens: bajar `<style>`/`<script>`, promover hex sueltos a `@theme inline`, `projects.ts` class-toggle. Route: delegated writer. Checks: vitest + build + playwright carousel. (`816ca14`, ver evidencia abajo)
- [x] P8 — Tipos y lib: aplanar `CarouselCertification`, const-objects para `status`/`category`/`Locale`/`ThemePreference`, `any` ambiental, documentar `lib/` real en README + checklist translationKey. Route: delegated writer. Checks: `pnpm check` + vitest. (`bcc8811`, ver evidencia abajo)
- [x] P9 — Supply-chain y SEO: `minimumReleaseAge`/dependabot npm/cooldown/lockfile-lint evaluación, pin Node exacto `22.x`, OG PNG junto a SVG, LHCI o asserts para blog/privacy/hreflang. Route: delegated writer. Checks: build + links + LHCI relevante. (`632b1b5`, ver evidencia abajo)
- [x] P10 — Verificación full + informe. Route: delegated verifier + parent spot-check. Checks: full gate. → CERRADO 2026-09-25: `pnpm test` 30 files 200/200, `pnpm exec playwright test --project=chromium` 137/137 tras fix `d711743` (smoke OG PNG). `pnpm build` VERDE 90 páginas, CSP 90/638/11.

## Progress
- 2026-09-25: documento creado. Punto de partida: rama `refactor/portfolio-theme-tokens-01`, 6 sucios, log `e94a65d` head. RDD off. Próximo: P0.
- 2026-09-25 P0 cerrado: los 6 sucios ya estaban commiteados como `649fd53 refactor(header): move header styles to utilities` (54+/179-, coincide con el diff descrito). Reconciliación con T9 parked (`portfolio-final-polish.md`): NO es el T9 original (ese borraba `header.css` 172 líneas y rompía `css-contracts`); es una variante menor que conserva `header.css` (33 líneas: solo `@custom-variant` scrolled/unscrolled/dark-scrolled, `@theme inline` nav-glass/shadow-nav*/header-cta, y `.nav-active::after` scoped) y mantiene el contrato `header.css` esperado por `tests/css-contracts.test.ts`. Coherente y seguro: cero `var()` en los 3 componentes (verificado con rg en class attrs), tokens nombrados con alpha dentro (rgba plano, sin color-mix), `font-code` globalizado en `portfolio-base.css` y removido el duplicado de `portfolio-hero.css`, `min-w-max` en menús. Sin toques a tests protegidos. Árbol tracked limpio; este commit docs(odd) registra la evidencia. Próximo: P1.

## Verification evidence
- P10 FAILED 2026-09-25 (verifier fresco, sin fixes): `git diff --check`: OK (sin output) · `pnpm build`: VERDE — astro check 0 errors, limits OK, 90 páginas, bundle OK (hero-webgl 511.4 KiB raw / 126.9 KiB gzip), CSP parity 90 HTML / 638 scripts / 11 hashes · `pnpm test`: 30 files, 200/200 · `pnpm exec playwright test --project=chromium`: 136 passed, 1 FAILED — `tests/smoke.spec.ts:108` "missing keys retain OG fallback", línea 118: espera `og:image` ~ `/og\/es\.svg\?title=/`, recibido `https://sebita.dev/og/es.png`. Causa: P9 cambió el default `og:image` a PNG estático (`Layout.astro` usa `/og/<locale>.png`) pero no actualizó esta aserción; P9 solo corrió `json-ld.spec.ts` 5/5, no el smoke completo. Comportamiento intencional P9, test stale — fix corresponde al writer (actualizar aserción a `/og/es.png`), fuera de alcance del verificador. firefox/webkit: no corridos (chromium full ya rojo; cobertura real = solo chromium). `gentle-ai review assess --base-ref origin/refactor/portfolio-theme-tokens-01 --json`: informativo, risk high (security signal en `tests/security-headers.test.ts` + shell_process en `ci.yml`), 73 paths / 4253 líneas candidatas; RDD off, no bloquea. P0–P9 hashes verificados presentes en `git log`: `a668ae8` P0, `8a72449` P1, `01738f4` P2, `2f8a8d4` P3, `7c6070b` P4, `5fb3661` P5, `3ac099d` P6, `816ca14` P7, `bcc8811` P8, `632b1b5` P9. Acumulado rama: 18 commits sobre origin, 70 files +1998/−2015. Sin push.
- P9 (`632b1b5 chore(supply): pin node exact, harden pnpm/dependabot, add OG PNG fallback + LHCI coverage`, 19 files +371/−56 incl. 2 PNG binaries): `git diff --check`: OK · TDD RED (nuevo `tests/supply-chain.test.ts` 3/5 fail: pin `22` vs `22.23.3`, sin ecosistema npm, sin `minimumReleaseAge`/`blockExoticSubdeps`/`trustPolicy`; nuevo `tests/seo-social.test.ts` colección falla por `src/lib/og-image` inexistente) → GREEN · `pnpm install --frozen-lockfile`: OK (`Lockfile is up to date`, hardening no rompe resolución) · `pnpm test`: 30 files, 200/200 · `pnpm build`: VERDE — 90 páginas, bundle OK (hero-webgl 511.4 KiB raw / 126.9 KiB gzip), CSP parity 90 HTML / 638 scripts / 11 hashes intacto (cero re-hash: ningún `<script>` inline tocado) · `pnpm validate:links`: OK primer intento (todos operativos) · Playwright chromium `json-ld.spec.ts`: 5/5 · LHCI real NO ejecutado localmente (sin Chrome/LHCI en entorno; corre en CI) — no se declara verde.
- Diseño supply: `.nvmrc`/`engines`/`ci.yml`/`lighthouse.yml` alineados a `22.23.3` exacto (era `22` vs `22.x` vs `22`); `packageManager pnpm@10.30.1` ya exacto y workflows ya lo pinean. `pnpm-workspace.yaml` suma `minimumReleaseAge: 20160` (14d, ventana LiteLLM/Telnyx <3h), `blockExoticSubdeps: true` (lockfile verificado 633/633 `resolution: {integrity:}`, cero tarball/git — nada que bloquear, seguro), `trustPolicy: no-downgrade` (señal takeover; frozen install verificado OK). `dependabot.yml` suma ecosistema npm semanal con cooldown 7d en los 4 niveles (github-actions semanal intacto). lockfile-lint evaluado y DESCARTADO como dependencia: el contrato `tests/supply-chain.test.ts` es el equivalente zero-dep (sin git:/tarball:/file: en manifest, toda resolución del registry con integrity hash) + `pnpm audit --prod` ya en CI + installs frozen. Contratos mantenidos en mismo unit: `pnpm-config` (workspace exacto extendido), `lighthouse-config` (node + urls + skipAudits), `github-actions-pinning` (regex github-actions + nuevo assert cooldown npm). Sin push.
- Diseño SEO: `src/lib/og-image.ts` nuevo (template SVG extraído verbatim del route — output byte-idéntico; unidad testeada: escape, cap 100 chars, fallback locale, canvas 1200×630); route adelgazado + `Cache-Control` alineado a `public, max-age=31536000, immutable` (paridad con regla vercel `/og/(.*)`, antes `604800` en conflicto). `scripts/generate-og-images.ts` + `pnpm generate:og` renderiza `public/og/es.png`+`en.png` (87/84 KiB, 1200×630 verificados por firma PNG+IHDR en test) vía sharp ya allowlisted; `Layout.astro` usa `/og/<locale>.png` como `og:image`/`twitter:image` por defecto (blog posts conservan su webp absoluta propia). Endpoint SVG vivo para URLs ya compartidas. `vercel.json` solo suma regla `/cv/(.*\.pdf)` espejo de certifications (P2 risk cerrado; validador CSP solo mira regla global, intacto). `lighthouserc.json`: urls +`/blog/`+`/privacy/` (rutas reales en sitemap: 86 URLs blog/privacy) y un-skip `canonical` — estable empíricamente: 90/90 HTML con `rel="canonical"` en `dist`, `robots-txt` sigue skippeado (mínimo). Trade-off documentado: PNG estático pierde título por-página del SVG dinámico, pero es lo único que X/FB/LinkedIn/WA renderizan.
- P8 (`bcc8811 refactor(types): flatten carousel types, derive unions from const-objects, drop ambient any`, 8 files +197/−24): `git diff --check`: OK · TDD RED (`tests/type-contracts.test.ts` 6/6 fail + `pnpm check` 8 errors: sin `LOCALES`/`PROJECT_STATUSES`/`CERTIFICATION_CATEGORIES`/`THEMES`/`CarouselCertificationData`, `any` en env.d.ts) → GREEN · `pnpm check`: 155 files 0/0/0 · `pnpm test`: 28 files, 186/186 · `pnpm build`: VERDE — 90 páginas, bundle OK (hero-webgl 511.4 KiB raw / 126.9 KiB gzip), CSP parity 90 HTML / 638 scripts / 11 hashes intacto (cero re-hash: ningún `<script>` inline tocado).
- Diseño: `CarouselCertificationData` plana + `CarouselCertification { data }` preserva contrato carousel (P7 intacto, cero cambios en `.astro`); `LOCALES` en `lib/i18n` (tupla `locales` + `defaultLocale` derivadas, `i18n.test.ts` intacto); `PROJECT_STATUSES`/`CERTIFICATION_CATEGORIES` en `lib/data/types`; `THEMES` en `@/types` con `import type` en `theme-toggle.ts` (bundle cliente byte-idéntico, CSP a salvo); `content.config.ts` deriva `z.enum` de `LOCALES`; `env.d.ts` usa `AstroComponentFactory` (patrón `tech-badges.ts`) + `declare global` para `Window`; re-exports con valor solo desde `data/types` liviano, nunca del facade. README: tabla `lib/` real + sección tipos + checklists idioma/página/post con `translationKey`. Sin push.
- P7 (`816ca14 refactor(carousel): slim styles to utilities, tokenize badges, extract script`, 9 files +187/−288): `git diff --check`: OK · TDD RED (projects-script `expected false to be true` en classList-hidden + design-tokens `hsl(144` literales sin `bg-cert-pro-bg`) → GREEN · `pnpm test`: 27 files, 180/180 · `pnpm build`: VERDE — limits OK (Carousel 451→191 líneas, excepción source-limits removida), 90 páginas, bundle OK (hero-webgl 511.4 KiB raw / 126.9 KiB gzip), CSP parity 90 HTML / 638 scripts / 11 hashes (2 swaps en mismo commit: carousel `zNLc/…`→`mGrUYu…`, projects `/pdBRsj…`→`B1vNz…`, cuerpos verificados en `dist/*/index.html`) · Playwright chromium `certification-carousel.spec.ts`: 5/5 + `css-contracts.spec.ts`: 4/4.
- Diseño: layout estático a utilities (`w-full`, `flex/gap`, `min-w/max-w` responsive, `px/py` responsive, `bg-card/border-border/text-*` nombrados, `bg-card/92` preserva alpha nav), `@theme inline` + `:root`/`.dark` con `--cert-pro`/`--cert-pro-text` (verdes intactos, `bg-cert-pro-bg/text/border`), `<style>` conserva solo masks/hover-shadows/gradients/backdrop/reduced-motion, script extraído a `src/scripts/cert-carousel.ts` (`initCertCarousel`+`teardown`, wrapper `DOMContentLoaded`+`astro:page-load`), `projects.ts` usa `classList.add("hidden")` (Tailwind `hidden` ya generado). Sin push.
- P6 (`3ac099d refactor(layout): extract JSON-LD and stable scripts from Layout.astro`, 6 files +287/−150): `git diff --check`: OK · TDD RED (4 tests nuevos `buildLayoutJsonLd` en `tests/json-ld.test.ts`: import inexistente) → GREEN · `pnpm test`: 27 files, 179/179 · `pnpm build`: VERDE — limits OK, 90 páginas, bundle OK (hero-webgl 511.4 KiB raw / 126.9 KiB gzip), CSP parity 90 HTML / 638 scripts / 11 hashes · Playwright chromium `smoke.spec.ts` + `json-ld.spec.ts`: 24/24 + `animation-behavior.spec.ts` + `site-pages-accessibility.spec.ts`: 11/11.
- P5 (`5fb3661 refactor(work): migrate work.ts to Shared + Record<Locale> pattern`, 3 files +131/−48): `git diff --check`: OK · TDD RED (naive "shares skills" en `tests/work-contracts.test.ts`: `expected [ 'Network Maintenance', … ] to deeply equal [ 'Mantenimiento de Redes', … ]`) → GREEN con contrato documentado · `pnpm exec vitest run tests/work-contracts.test.ts tests/data-modules.test.ts tests/performance-contracts.test.ts`: 3 files, 13/13 · `pnpm test`: 27 files, 174/174 · `pnpm build`: VERDE — `astro check` 151 files 0/0/0, limits OK, 90 páginas, bundle OK (hero-webgl 511.4 KiB raw / 126.9 KiB gzip), CSP parity 90 HTML / 638 scripts / 11 hashes (intacto) · render verificado en `dist`: `Contrato por proyecto (3 meses)` + `Mantenimiento de Redes` en ES, `Full Stack Developer` + `Network Maintenance` en EN (copy/fechas intactos).
- Diseño: `SharedWork` = company/fechas/current/logos (idénticos), `LocalizedWork` = position/description/location/employmentType/skills/highlights/url; clones por locale como `projects.ts` (sin estado mutable compartido). Divergencias preservadas con comentario, no normalizadas (sin autorización de copy): Temutel skills traducidos (vs `tags` compartidos en projects) y employmentType ES largo vs EN `Contract`. Fix inducido: `performance-contracts.test.ts:19` `?? []` — `WorkExperience.logos` siempre fue opcional en el tipo canónico; el inferido anterior lo ocultaba; aserción intacta (3 logos). Sin push.
- P4 (`7c6070b refactor(privacy): dedupe ES/EN pages into LocalizedPrivacy component`, 6 files +698/−633): `git diff --check`: OK · TDD RED (`ENOENT src/components/LocalizedPrivacy.astro` en `tests/privacy-content.test.ts`) → GREEN · `pnpm exec vitest run tests/privacy-content.test.ts tests/i18n.test.ts tests/pages-smoke.test.ts tests/ui-copy-localization.test.ts`: 4 files, 32/32 · `pnpm test`: 26 files, 170/170 · `pnpm build`: VERDE — `astro check` 150 files 0/0/0, limits OK, 90 páginas, bundle OK (hero-webgl 511.4 KiB raw / 126.9 KiB gzip), CSP parity 90 HTML / 638 scripts / 11 hashes (intacto, privacy sin scripts inline) · Playwright chromium `privacy.spec.ts` + `smoke.spec.ts`: 23/23 + `site-pages-accessibility.spec.ts`: 8/8 · render verificado en `dist`: 8 `<section>` por locale, 0 menciones "Vercel", alternates locale-first con x-default ES, h1/email intactos.
- Alcance copy (autorizado en P4): sec 3 reescrita "Analítica y Medición"/"Analytics and Measurement" (postura minimal-collection: sin píxeles, sin identificadores, sin terceros; nota de actualización previa si se reintroduce medición), bullet IP sec 2 sin "anonimizadas por Vercel", párrafo base-legal GDPR sin Vercel Analytics, descriptions sin mención Vercel, `lastUpdate` 25-sep-2026 (la política cambió). Resto legal verbatim por locale, incluido orden ARCO/GDPR propio de cada locale (swap condicional, cero reestructuración). Excepción source-limits "legal content document" movida de los 2 wrappers al componente (P1 precedent); wrappers de 5 líneas. Sin `var()` en className, clases/utilidades idénticas (identidad visual intacta). Sin push.- P3 (removal, quitar): `git diff --check`: OK (sin output) · baseline `pnpm exec vitest run tests/lib-contracts.test.ts tests/projects-script.test.ts tests/security-headers.test.ts`: 3 files, 25/25 GREEN → RED tras borrar `src/lib/analytics.ts` (`Cannot find module '../src/lib/analytics'`, `Failed to resolve import "@/lib/analytics"`, security-headers `expected [...] to include 'https://va.vercel-scripts.com'`) → GREEN 23/23 tras podar tests (lib-contracts 3, projects-script 2, security-headers 18) · `pnpm install --lockfile-only` (solo prune, 0 descargas) + `pnpm install --frozen-lockfile` OK (`node_modules/@vercel` eliminado) · `pnpm test`: 25 files, 164/164 passed · `pnpm build`: VERDE — `astro check` 148 files 0/0/0, limits OK, 90 páginas, bundle OK (hero-webgl 511.4 KiB raw / 126.9 KiB gzip), CSP parity 90 HTML / 638 scripts / 11 hashes · Playwright chromium `smoke.spec.ts` 19/19 + `project-blog-destinations.spec.ts` 2/2 (selectores `data-track-*` intactos) · grep final `vercel/analytics|speed-insights|trackEvent|vitals|va.vercel-scripts` en src/tests/scripts/configs: 0 refs (solo aserción negativa intencional en security-headers) · commit work-unit abajo, sin push.
- Muerto confirmado (evidencia): `build:static` = `env -u VERCEL ... astro build` (`package.json:13`) → `import.meta.env.VERCEL` siempre falso → `<Analytics/>`/`<SpeedInsights/>` + 2 preconnects en `Layout.astro:145,295-296` nunca renderizan; `track()` de `@vercel/analytics` solo encola en `window.va` (`node_modules/@vercel/analytics/dist/index.js:186`), y sin `inject()` del componente `window.va` es `undefined` → `trackEvent` era no-op silencioso en Hero + projects. Se pagaba: 2 deps + lock, bundle cliente, `script-src va.vercel-scripts.com` + `connect-src vitals + va`.
- `vercel.json` tocado SOLO en valor CSP (owns `security-headers`, out of scope en lo demás): `script-src` −`https://va.vercel-scripts.com`, `connect-src` → `'self'`, re-hash 10→11 en mismo commit (bundle inline de `Projects.astro` adelgazado al quitar tracking: hash nuevo `sha256-/pdBRsj5r2D56kKHvA/...`, cuerpo verificado en `dist/*/index.html`); `scripts/verify-csp-hashes.ts` + `tests/security-headers.test.ts` actualizados en parity 1:1. Hashes restantes intactos y en orden.
- Alcance deliberado: `data-track-*` en markup SE CONSERVAN (son selectores E2E en `smoke.spec.ts:36,42-43` y `project-blog-destinations.spec.ts:22`); copy `privacy.astro`+`en/privacy.astro` sobre Vercel Analytics intacto (lo reescribe P4, copy sin autorización no se toca); blog MDX menciona Google Analytics en contexto WordPress ajeno (no se toca).
- Bug latente detectado FUERA de alcance (no se toca en P3): doble `setupProjects()` (vía `astro:page-load` en `Projects.astro:227` sin `before-swap` intermedio) aborta listeners spotlight y el guard `spotlightBound` impide re-bind → spotlight muere en silencio. Pre-existente, independiente de este cambio. Follow-up propuesto.
- P2 (`01738f4`): `git diff --check`: OK (sin output) · `git check-ignore -v odd/tasks/portfolio-audit-followup.md odd/new-probe.md`: ignorados (`odd/tasks/*`, `odd/*`) · `git check-ignore -v odd/tasks/portfolio-perfection.md`: exit 1 (no ignorado, sigue trackeado) · `pnpm exec vitest list`: colección intacta (las 5 líneas fail/error son títulos de tests, no fallos) · `pnpm exec playwright test --list`: `capture-screenshots` 0 matches, `regression-contracts.spec.ts` 3 tests × 3 browsers · `pnpm test`: 25 files, 166/166 passed · `pnpm build`: VERDE — 90 páginas, bundle OK (hero-webgl 511.4 KiB raw / 126.9 KiB gzip), CSP parity 90 HTML / 636 scripts / 10 hashes, `dist/cv/Sebastian_Bravo_CV.pdf` servido y linkeado como `/cv/...` en index.html · PR #37 odd-diff: 8 files/402 insertions → 1 file/83 insertions (solo el tracker).
- Nota gitignore: `odd/` + negación simple NO funciona (git no desciende a dirs excluidos); se usó cascada `odd/*` / `!odd/tasks` / `odd/tasks/*` / `!odd/tasks/portfolio-perfection.md`, verificada empíricamente. `git rm --cached` solo saca del índice: los 7 docs siguen en disco (ahora ignorados), cero pérdida. `vercel.json` intacto (owns `security-headers`, out of scope); `/cv/*.pdf` queda sin regla immutable-cache (solo existe para `/certifications/*.pdf`) — follow-up opcional.
- P1 (`8a72449`): `git diff --check`: OK (sin output) · `pnpm exec vitest run tests/tech-badges.test.ts tests/lib-contracts.test.ts`: 2 files passed, 8/8 tests passed · `pnpm build`: VERDE — `astro check` 149 files 0/0/0, limits OK, 90 páginas, bundle OK (hero-webgl 511.4 KiB raw / 126.9 KiB gzip), CSP parity 90 HTML / 636 scripts / 10 hashes · `git log --oneline -2`: `8a72449` + `a668ae8` · RED intermedio observado: `Cannot find module '../src/lib/tech-colors'` en lib-contracts tras borrar, antes de podar.
- P0 (`a668ae8`): `git diff --check`: OK (sin output; `git status --short` solo muestra este doc untracked, cero modificados tracked)
- `pnpm exec vitest run tests/css-contracts.test.ts tests/theme-utilities.test.ts tests/theme-toggle.test.ts tests/language-toggle.test.ts`: 4 files passed, 16/16 tests passed
- `pnpm build`: VERDE — `astro check` 150 files 0/0/0, limits OK, 90 páginas, bundle OK (hero-webgl 511.4 KiB raw / 126.9 KiB gzip), CSP parity 90 HTML / 636 scripts / 10 hashes
- `git log --oneline -3`: ver hash de este commit docs(odd) abajo
- Nota TDD strict: refactor estilos→utilities sin cambio de comportamiento; este run no introdujo cambios fuente, por lo que no hubo RED previo observable — GREEN verificado post-hoc. Sin evidencia inventada. RED/GREEN de behavior changes aplica desde P1.
- 2026-09-25 P1 cerrado (`8a72449 refactor(tech): remove dead tech-colors registry`, 4 files +3/−326). Muerto confirmado: `rg getTechStyle|tech-colors` en src/tests/scripts solo matcheaba `tests/lib-contracts.test.ts` (import) + `scripts/check-source-limits.ts` (excepción stale) + el propio archivo; cero importadores en `src/` (`Projects.astro:160` usa `getTechBadge` de `tech-badges`). Archivos: borrado `src/lib/tech-colors.ts` (311 líneas), podado import + bloque "normalizes known and unknown technology styles" en `tests/lib-contracts.test.ts` (resto de contratos intacto), quitada excepción stale en `scripts/check-source-limits.ts`, actualizada línea de DESIGN.md §11 (ya no cita "canonical technology color registry"; nombra `tech-badges.ts` como canónico). TDD: BASELINE GREEN 9/9 → RED tras borrar (lib-contracts "Cannot find module '../src/lib/tech-colors'", tech-badges 4/4 intacto) → GREEN 8/8 tras podar. Próximo: P2.

## Next step
- Writer fix mínimo: actualizar `tests/smoke.spec.ts:118` a `/og/es.png` (assert del default P9) y re-correr `pnpm exec playwright test tests/smoke.spec.ts --project=chromium`; luego re-verificar P10 para cierre.
- 2026-09-25 writer GREEN: `tests/smoke.spec.ts:118` actualizado a `/\/og\/es\.png/` (flexible: absoluto o relativo, con o sin query). `pnpm exec playwright test tests/smoke.spec.ts --project=chromium`: 19/19 passed. `git diff --check`: OK. P10 sigue [ ] hasta full gate de cierre.

## Route declaration
- P0–P9: delegated direct (writer trigger: 2+ files no triviales; preparation trigger: lectura prepara escritura). P10: parent spot-check + verifier on-demand. Ninguna ruta crea artefactos SDD ni invoca `sdd-*`.
