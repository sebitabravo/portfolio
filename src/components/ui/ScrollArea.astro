---
import { cn } from "@/lib/utils"

interface Props {
  class?: string
}

const { class: className } = Astro.props
---

<div class={cn("overflow-auto rounded-lg border border-border bg-card", className)}>
  <slot />
</div>
