---
// Section Container — Superhuman generous spacing
// Max-width 1200px, clean sections
const { class: className, ...rest } = Astro.props
---

<section {...rest} class:list={["w-full mx-auto max-w-[1200px]", className]}>
  <slot />
</section>
