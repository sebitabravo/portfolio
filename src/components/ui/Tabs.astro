---
interface Props {
  defaultValue?: string;
  class?: string;
}

const { defaultValue = '', class: className = '' } = Astro.props;
---

<div class={`tabs-root ${className}`} data-default-value={defaultValue}>
  <slot />
</div>

<style>
  .tabs-root {
    @apply w-full;
  }
</style>
