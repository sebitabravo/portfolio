# Portfolio maintainability pass 3

## Goal
Apply the user's review corrections conservatively, prioritizing Button color-token clarity, then evidence-backed CSS cleanup, then genuinely dead code/assets and audit documentation. Preserve existing worktree changes, visual identity, ES/EN behavior, accessibility, SEO, and responsive/reduced-motion behavior. The user now explicitly accepts a single oversized PR with `size:exception`; publish all accumulated changes to approved issue #35 and do not merge. The native archive of `security-headers` remains pending.

## Baseline and constraints
- The accumulated implementation began on `odd/portfolio-design-architecture`; the current worktree is on `refactor/portfolio-theme-tokens-01`. Preserve all remaining dirty changes. Do not reset, clean, or overwrite unrelated changes. The user explicitly approved placing the complete accumulated diff in one PR despite exceeding 400 changed lines.
- The review's Button finding is partly inaccurate: `Button.astro` currently uses arbitrary Tailwind syntax that references semantic HSL variables, not hardcoded arbitrary colors. Improve readability using the canonical theme utilities only if generated styles and behavior remain equivalent.
- Do not delete historical/user PDFs or narrow reusable component APIs based only on current call sites. Investigate direct/external uses first; request explicit confirmation before irreversible asset deletion.
- Keep justified responsive/reduced-motion cascade overrides. A count of `!important` is not sufficient evidence to remove them.
- Keep public package script aliases and security dependency overrides unless repo evidence demonstrates an actual defect and migration is safe.
- Native SDD status for `security-headers` returned `nextRecommended: archive`, all 14 tasks complete, and no blockers. SDD preflight was confirmed in this session (auto, hybrid, auto-chain, 400 lines); native store authority remains OpenSpec. The `sdd-archive` executor is not available in the current agent inventory; do not manually move/archive artifacts as a substitute.

## Tasks
- [x] Canonicalize Button token classes and add focused contract coverage; verify default/secondary/outline/link colors remain semantic-token-backed in light and dark themes. `Button.astro` now uses named theme classes including `hover:bg-primary/88` and `hover:bg-secondary/80`; the arbitrary HSL-alpha form is removed. The source tests now reject the old form and enforce hover-only opacity. RED 1 failure; GREEN 147 tests. Independent review found no issue. `pnpm build` passed (90 pages, 157 Astro files/0 diagnostics, source/bundle/CSP checks). A delegated inspection of `dist/_astro/Layout.DditlY5x.css` confirmed emitted hover rules use `color-mix(in oklab, hsl(var(--primary)) 88%, transparent)` and corresponding 80% secondary opacity, with fallback declarations. Commit `968ebaa377c60c6383e5f4635cd170a5a150cd14`; child PR #37.
- [x] Audit CSS duplication and custom-property usage across the repo. Removed only the six proven-unreferenced `--hero-gradient-*` and `--nav-*` declarations from both palettes. Limited Button transitions to color/background/border/transform and removed the overlapping `transition: all` declaration. Kept focus cues. Retained `!important`: reduced-motion/mobile rules must override utilities, and `.section-card` styles must override repeated component utility classes in the lower-priority component layer. Full Vitest verification passed 23 files/147 tests. Included in commit `968ebaa377c60c6383e5f4635cd170a5a150cd14` and child PR #37.
- [x] Resolve only confirmed dead-code/documentation findings. Corrected `odd/tasks/portfolio-audit-cleanup.md`: it now states `components.json` is present, `lighthouse-scores.png` is absent, and old `devalue`/blog findings were historical and resolved. Other candidates remain because evidence does not justify removal: two unreferenced certification PDFs may be intentional historical assets; `CertificationCarousel`'s logo branch is unused by its sole current caller but remains a reusable optional API; the Rollup override is an active security dependency override; `check` and `test:e2e` are documented local aliases; candidate locale keys have ambiguous consumers. Dynamic project gradients are a finite slug map with a fallback, not arbitrary CSS. No assets or dependency overrides were deleted.
- [ ] Complete the native OpenSpec archive phase for `security-headers` after the cleanup, once the designated `sdd-archive` executor is available. Native status says ready; the current agent inventory has no `sdd-archive` executor. Do not hand-archive.
- [x] Run proportionate focused checks and delegated verification/review for changed code; record exact outcomes, limitations, and worktree status. Final `pnpm build` passed: 157 Astro files/0 diagnostics, 90 pages, source/bundle/CSP checks, CSP parity 90 HTML/636 scripts/10 hashes. Full Vitest passed 23 files/147 tests. Full Playwright passed 273/273 (Chromium 91, Firefox 90, WebKit 92); no console/page errors were reported. `pnpm audit --prod` and `git diff --check` passed. Generated-CSS inspection confirmed Button's 88%/80% hover opacity rules. Button LSP is unavailable; six `no-important` advisories in existing reduced-motion rules were retained intentionally. Published PR #37 to `main` with 4,978 changed lines across 84 files; user explicitly accepted `size:exception`. Commits: `3630b57` tracker, `968ebaa` theme tokens, `4ef9d04` complete portfolio pass. Vercel deployment/comment checks passed; PR remains open, not merged. Superseded tracker PR #36 is closed. Native archive remains pending because `sdd-archive` is unavailable.

## Follow-up: Hosted Firefox validation failure
- Evidence: GitHub `validate` run `35937518477`, `Run E2E tests`, failed only at `tests/home/home.spec.ts:379` in Firefox after all retries. Expected `data-motion-status` to reach `active|fallback`; it remained `loading` for 10 seconds. 272 other tests passed. Local Playwright had passed 273/273 before the latest docs-only follow-up.
- Read-only investigation: `setupPortfolioMotion()` cancels the inline 3-second fallback when it claims the root, then relies on IntersectionObserver to invoke `startLoading()`. Its 4-second fallback timer is only armed after that callback. If observer activation is not delivered, status can remain `loading` indefinitely. Existing no-observer path already checks geometry synchronously.
- Investigation found two startup gaps: a delayed observer callback can leave an in-range hero dormant; if the hero is out of range, the module cancels Hero's inline fallback before deferred activation, so status may remain `loading`. Latest failed CI run had no uploaded artifacts; exact hosted geometry remains unknown.
- Strict-TDD delegated fix one added an immediate proximity check after observer registration; near-hero unit and focused Firefox tests passed locally, but hosted Firefox still failed. Strict-TDD delegated fix two adds a browser regression that forces the hero outside the observer margin and suppresses only its callback; it reproduced `loading` before the change and passed after Hero's 3-second inline fallback remained armed after module claim. Deferred activation remains available later.
- Strict-TDD ownership correction: `tests/motion-fallback.spec.ts` now dispatches `portfolio-motion:started` for actual activation, not mere claim; RED observed fallback replacing loading (1 failed/1 passed). `startLoading()` emits `started`; Hero clears the inline timer only on that event, so it remains armed during deferred startup. Exact build-observed hash in `vercel.json`: `sha256-tIFqFv2zuoiBT9/DvHrNAJGymAYHRyJ1lhG7VBWm0sA=`; no CSP directive/source was weakened. Source-limit check passes at 300 lines. The authorized build/test outputs and exact build sequence are recorded in the Engram task mirror.
- Delegated GREEN and independent verification passed: focused ownership/offscreen Firefox coverage (3 tests), `git diff --check`, full build (90 pages; CSP parity 90 HTML/636 scripts/10 hashes), Vitest 23 files/148 tests, and Playwright 276/276 across Chromium, Firefox, and WebKit. Vite reported only the existing Vitest `esbuild` deprecation warning. Next commit/push, refresh PR #37's exact GitHub size/body, and watch hosted checks. Keep PR #37 open; do not merge.

## Progress
- 2026-09-24: Read current review findings and reconciled the dirty worktree. Independent scout found Button colors already reference semantic tokens; candidate unused CSS variables require a full repo reference check; three `.section-card` `!important` declarations warrant analysis but are not proven unnecessary; two certification PDFs are unreferenced in current data but may be intentional; `components.json` exists while `lighthouse-scores.png` is absent. Native status reports security-headers archive ready; designated archive executor unavailable.
- 2026-09-24: Completed Button utility cleanup test-first. Named theme utilities replace arbitrary `hsl(var(...))` classes while alpha hover classes are deliberately preserved. Delegated Vitest verification passed 23 files/145 tests.
- 2026-09-24: Completed CSS cleanup test-first: removed six properties proven unreferenced repo-wide and replaced Button's broad transition override with an explicit transition-property utility, preserving focus and active states. RED 2 failures; GREEN 147 tests. Independent verifier passed full Vitest 23 files/147 tests. Build/visual comparison is pending. Removed no `!important` because the declarations serve responsive/reduced-motion overrides or intentionally beat repeated utility classes.
- 2026-09-24: Corrected stale audit status and classified remaining dead-code candidates conservatively. Updated the audit ledger; kept personal PDFs, optional logo API, security override, and public script aliases. Dynamic gradients are bounded by a finite map. Native security-headers archive remains blocked only by missing SDD archive executor.
- 2026-09-24: Independent review caught the old malformed-looking Tailwind arbitrary opacity values; the initial tests had asserted those strings. Replaced them with named slash-opacity utilities, strengthened the contract, and reran RED/GREEN. The independent review found no further issue. Build passed; generated CSS inspection confirmed real 88%/80% `color-mix()` hover declarations.
- 2026-09-24: Final delegated verification passed `pnpm build` (90 pages, 157 Astro files/0 diagnostics, source/bundle/CSP checks; 90 HTML/636 scripts/10 hashes), full Vitest 23 files/147 tests, and `git diff --check`.
- 2026-09-24: User explicitly accepted one oversized PR with `size:exception`. Committed the remaining accumulated changes as `4ef9d04`, retargeted PR #37 to `main`, updated the PR body with the then-current GitHub size, and closed tracker #36 as superseded. PR #37 remains open; no merge. Native archive remains pending because `sdd-archive` is unavailable.
- 2026-09-24: Fixed the hosted Firefox motion-state failure via a delegated strict-TDD regression and synchronous near-hero proximity check after IntersectionObserver registration. Focused Vitest 17/17 and focused Firefox E2E 1/1 passed. After removing a nonessential comment to satisfy the 300-line source cap, final independent checks passed: build 90 pages/CSP parity 90/636/10, Vitest 23 files/148 tests, Playwright 273/273 (Chromium 91, Firefox 90, WebKit 92). This correction remains uncommitted/unpushed; rerun hosted checks after it is pushed.
